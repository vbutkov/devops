 Задание:

1. Запустить интерактивно образ ubuntu и открыть консоль bash внутри контейнера

• docker run -i -t ubuntu /bin/bash

2. Запустить контейнер docker/getting-started в фоновом режиме с мапингом портов 80:80

• docker run -d -p 80:80 docker/getting-started

3. Найти контейнер в системе командой docker ps и узнать id. Использовав id, остановить контейнер

• docker ps ; docker stop id

4. Запустить локальный registry и запушить в него образ, как на последнем слайде

 • Инструкция на последнем слайде.
